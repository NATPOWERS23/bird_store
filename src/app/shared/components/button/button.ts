export enum ButtonType {
  DEFAULT = 'default',
  OUTLINE = 'outline',
  CONTRAST = 'contrast',
}

export enum ButtonSize {
  LARGE = 'large',
  MEDIUM = 'medium',
  SMALL = 'small',
}

export enum IconType {
  DEFAULT = 0,
  CHECKED = 1,
}
